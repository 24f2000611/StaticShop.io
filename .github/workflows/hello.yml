name: My First Action 

on: [push]  

jobs:
  test:
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout code 
        uses: actions/checkout@v4
        
      - name: 24f2000611@ds.study.iitm.ac.in  
        run: echo "Hello from my GitHub Action!"  
        
      - name: List files 
        run: ls -la
